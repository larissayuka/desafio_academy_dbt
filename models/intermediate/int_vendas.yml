version: 2

models:
  - name: int_vendas
    description: modelo intermediário que faz a relação das vendas.
    columns:
      - name: sk_venda
        description: chave surrogada da tabela vendas.
        tests:
          - unique
          - not_null